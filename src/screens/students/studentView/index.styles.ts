import { themeStyles } from "@common/theme";

export const useStyles = themeStyles.create(() => {
  return {
    containList: {
      paddingTop: 20,
      paddingHorizontal: 20,
      paddingBottom: 70,
    },
  };
});
