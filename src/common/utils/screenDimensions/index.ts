import { Dimensions } from "react-native";

export const { width: globalScreenWidth, height: globalScreenHeight } =
  Dimensions.get("screen");

export const { width: globalWindowWidth, height: globalWindowHeight } =
  Dimensions.get("window");
